config:
  cwd: './external'

product:

  name: "product"
  cores:
    core0:
      name: 'main'
      device: 'sim'
      conf_path: './external/nuttx/.config'
      elf_path: './external/nuttx/nuttx'
